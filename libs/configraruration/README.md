# configraruration

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build configraruration` to build the library.

## Running unit tests

Run `nx test configraruration` to execute the unit tests via [Jest](https://jestjs.io).
